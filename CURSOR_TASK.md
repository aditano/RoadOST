# Cursor task

Implement RoadOST exactly as specified in SPEC.md.

Working directory: this repo.

Do all of it:

1. Scaffold Vite + TypeScript + Tone.js + Vitest
2. Implement sensors, mapper, audio engine, simulator, HUD, export
3. Write mapper unit tests
4. Add GitHub Actions CI + Pages-ready Vite config (`base: '/RoadOST/'`)
5. Replace README with the real product README
6. Run `npm test` and `npm run build` and fix until both pass

Stop only when acceptance in SPEC.md is met. Summarize files changed and the verify commands you ran.
