# Agent instructions

Read `SPEC.md` and implement it fully.

- Do not switch stacks.
- Do not add a live music-generation API.
- Do not commit secrets or `node_modules`.
- User-facing copy: no em dashes.
- Keep working until `npm test` and `npm run build` both pass.
- After the app works, write the real README (the current one is a placeholder).
